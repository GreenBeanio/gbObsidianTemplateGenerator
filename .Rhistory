} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
input_date <- as.Date("2026-01-05")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
first_monday <- input_date - lubridate::days(w_day) + 1
print(lubridate::wday(first_monday, week_start = 1))
} else {
first_monday <- input_date
}
if(lubridate::year(first_monday) == lubridate::year(input_date)) {
input_date <- first_monday
} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
input_date <- as.Date("2026-01-03")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
first_monday <- input_date - lubridate::days(w_day) + 1
print(lubridate::wday(first_monday, week_start = 1))
} else {
first_monday <- input_date
}
if(lubridate::year(first_monday) == lubridate::year(input_date)) {
input_date <- first_monday
} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
input_date <- as.Date("2026-01-03")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
first_monday <- input_date - lubridate::days(w_day) + 1
} else {
first_monday <- input_date
}
if(lubridate::year(first_monday) == lubridate::year(input_date)) {
input_date <- first_monday
} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
input_date <- as.Date("2026-01-03")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
first_monday <- input_date - lubridate::days(w_day) + 1
} else {
first_monday <- input_date
}
if(lubridate::year(first_monday) == lubridate::year(input_date)) {
input_date <- first_monday
} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
input_date <- as.Date("2026-01-04")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
first_monday <- input_date - lubridate::days(w_day) + 1
} else {
first_monday <- input_date
}
if(lubridate::year(first_monday) == lubridate::year(input_date)) {
input_date <- first_monday
} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
input_date <- as.Date("2026-01-03")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
first_monday <- input_date - lubridate::days(w_day) + 1
} else {
first_monday <- input_date
}
if(lubridate::year(first_monday) == lubridate::year(input_date)) {
input_date <- first_monday
} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
input_date <- as.Date("2026-12-28")
# Prevent the week starting in the previous year
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
first_monday <- input_date - lubridate::days(w_day) + 1
} else {
first_monday <- input_date
}
if(lubridate::year(first_monday) == lubridate::year(input_date)) {
input_date <- first_monday
} else {
input_date <- input_date - lubridate::days(lubridate::mday(input_date) - 1)
}
# Prevent the week ending in the subsequent year
end_date <- input_date + lubridate::weeks(1)
w_day <- lubridate::wday(end_date, week_start = 1)
end_date <- input_date + lubridate::weeks(1)
if(lubridate::year(first_monday) != lubridate::year(input_date)) {
m_day <- lubridate::mday(end_date)
}
end_date <- input_date + lubridate::weeks(1)
if(lubridate::year(end_date) != lubridate::year(input_date)) {
m_day <- lubridate::mday(end_date)
}
end_date <- input_date + lubridate::weeks(1)
if(lubridate::year(end_date) != lubridate::year(input_date)) {
m_day <- as.numeric(lubridate::mday(end_date))
}
# Prevent the week ending in the subsequent year
end_date <- input_date + lubridate::weeks(1)
if(lubridate::year(end_date) != lubridate::year(input_date)) {
m_day <- as.numeric(lubridate::mday(end_date))
end_date <- end_date - lubridate::days(m_day)
}
end_date <- input_date + lubridate::weeks(1)
if(lubridate::year(end_date) != lubridate::year(input_date)) {
#m_day <- as.numeric(lubridate::mday(end_date))
#end_date <- end_date - lubridate::days(m_day)
end_date <- lubridate::ceiling_date(input_date, unit = "week")
}
end_date <- input_date + lubridate::weeks(1)
if(lubridate::year(end_date) != lubridate::year(input_date)) {
#m_day <- as.numeric(lubridate::mday(end_date))
#end_date <- end_date - lubridate::days(m_day)
end_date <- lubridate::ceiling_date(input_date, unit = "month") - lubridate::days(1)
}
if(lubridate::year(end_date) != lubridate::year(input_date)) {
m_day <- as.numeric(lubridate::mday(end_date))
end_date <- end_date - lubridate::days(m_day)
#end_date <- lubridate::ceiling_date(input_date, unit = "month") - lubridate::days(1)
}
if(lubridate::year(end_date) != lubridate::year(input_date)) {
m_day <- as.numeric(lubridate::mday(end_date))
end_date <- end_date - lubridate::days(m_day)
#end_date <- lubridate::ceiling_date(input_date, unit = "month") - lubridate::days(1)
}
source(file.path(here::here(), "Functions.R"))
source(file.path(here::here(), "Utils.R"))
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
# if(!dir.exists(output_dir)) {
#   dir.create(output_dir)
#   dir.create(file.path(output_dir, "Year"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Month"))
#   dir.create(file.path(output_dir, "Week"))
#   dir.create(file.path(output_dir, "Day"))
# }
#
# dates_df <-
#   tibble::tibble(Date = seq.Date(as.Date("2026-07-26"), Sys.Date(),
#                                  by = "day")) |>
#   dplyr::mutate(Quarter = lubridate::quarter(Date),
#                 Month = lubridate::month(Date),
#                 Week = lubridate::week(Date),
#                 Day = lubridate::wday(Date, label = TRUE, abbr = FALSE, week_start = 1))
#
# test_date <- dates_df |> dplyr::pull(Date) |> dplyr::first()
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- as.Date("2026-02-01")
# createYear(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
createMonth(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir)
#
# createWeek(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
source(file.path(here::here(), "Functions.R"))
source(file.path(here::here(), "Utils.R"))
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
# if(!dir.exists(output_dir)) {
#   dir.create(output_dir)
#   dir.create(file.path(output_dir, "Year"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Month"))
#   dir.create(file.path(output_dir, "Week"))
#   dir.create(file.path(output_dir, "Day"))
# }
#
# dates_df <-
#   tibble::tibble(Date = seq.Date(as.Date("2026-07-26"), Sys.Date(),
#                                  by = "day")) |>
#   dplyr::mutate(Quarter = lubridate::quarter(Date),
#                 Month = lubridate::month(Date),
#                 Week = lubridate::week(Date),
#                 Day = lubridate::wday(Date, label = TRUE, abbr = FALSE, week_start = 1))
#
# test_date <- dates_df |> dplyr::pull(Date) |> dplyr::first()
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- as.Date("2026-12-01")
# createYear(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
createMonth(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir)
#
# createWeek(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
source(file.path(here::here(), "Functions.R"))
source(file.path(here::here(), "Utils.R"))
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
# if(!dir.exists(output_dir)) {
#   dir.create(output_dir)
#   dir.create(file.path(output_dir, "Year"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Month"))
#   dir.create(file.path(output_dir, "Week"))
#   dir.create(file.path(output_dir, "Day"))
# }
#
# dates_df <-
#   tibble::tibble(Date = seq.Date(as.Date("2026-07-26"), Sys.Date(),
#                                  by = "day")) |>
#   dplyr::mutate(Quarter = lubridate::quarter(Date),
#                 Month = lubridate::month(Date),
#                 Week = lubridate::week(Date),
#                 Day = lubridate::wday(Date, label = TRUE, abbr = FALSE, week_start = 1))
#
# test_date <- dates_df |> dplyr::pull(Date) |> dplyr::first()
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- as.Date("2026-01-01")
# createYear(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
createMonth(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir)
#
# createWeek(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
source(file.path(here::here(), "Functions.R"))
source(file.path(here::here(), "Utils.R"))
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
# if(!dir.exists(output_dir)) {
#   dir.create(output_dir)
#   dir.create(file.path(output_dir, "Year"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Month"))
#   dir.create(file.path(output_dir, "Week"))
#   dir.create(file.path(output_dir, "Day"))
# }
#
# dates_df <-
#   tibble::tibble(Date = seq.Date(as.Date("2026-07-26"), Sys.Date(),
#                                  by = "day")) |>
#   dplyr::mutate(Quarter = lubridate::quarter(Date),
#                 Month = lubridate::month(Date),
#                 Week = lubridate::week(Date),
#                 Day = lubridate::wday(Date, label = TRUE, abbr = FALSE, week_start = 1))
#
# test_date <- dates_df |> dplyr::pull(Date) |> dplyr::first()
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- as.Date("2026-01-01")
# createYear(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createMonth(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
createWeek(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
source(file.path(here::here(), "Functions.R"))
source(file.path(here::here(), "Utils.R"))
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
# if(!dir.exists(output_dir)) {
#   dir.create(output_dir)
#   dir.create(file.path(output_dir, "Year"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Month"))
#   dir.create(file.path(output_dir, "Week"))
#   dir.create(file.path(output_dir, "Day"))
# }
#
# dates_df <-
#   tibble::tibble(Date = seq.Date(as.Date("2026-07-26"), Sys.Date(),
#                                  by = "day")) |>
#   dplyr::mutate(Quarter = lubridate::quarter(Date),
#                 Month = lubridate::month(Date),
#                 Week = lubridate::week(Date),
#                 Day = lubridate::wday(Date, label = TRUE, abbr = FALSE, week_start = 1))
#
# test_date <- dates_df |> dplyr::pull(Date) |> dplyr::first()
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- as.Date("2026-01-01")
# createYear(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createMonth(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
createWeek(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
input_date <- as.Date("2026-01-01")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
end_date <- input_date + lubridate::days(7 - w_day)
} else {
end_date <- input_date + lubridate::weeks(1)
}
input_date <- as.Date("2026-01-01")
w_day <- lubridate::wday(input_date, week_start = 1)
if(w_day != 1) {
end_date <- input_date + lubridate::days(7 - w_day)
} else {
end_date <- input_date + lubridate::weeks(1)
}
source(file.path(here::here(), "Functions.R"))
source(file.path(here::here(), "Utils.R"))
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
# if(!dir.exists(output_dir)) {
#   dir.create(output_dir)
#   dir.create(file.path(output_dir, "Year"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Month"))
#   dir.create(file.path(output_dir, "Week"))
#   dir.create(file.path(output_dir, "Day"))
# }
#
# dates_df <-
#   tibble::tibble(Date = seq.Date(as.Date("2026-07-26"), Sys.Date(),
#                                  by = "day")) |>
#   dplyr::mutate(Quarter = lubridate::quarter(Date),
#                 Month = lubridate::month(Date),
#                 Week = lubridate::week(Date),
#                 Day = lubridate::wday(Date, label = TRUE, abbr = FALSE, week_start = 1))
#
# test_date <- dates_df |> dplyr::pull(Date) |> dplyr::first()
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- as.Date("2026-01-01")
# createYear(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createMonth(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
createWeek(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
source(file.path(here::here(), "Functions.R"))
source(file.path(here::here(), "Utils.R"))
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
# if(!dir.exists(output_dir)) {
#   dir.create(output_dir)
#   dir.create(file.path(output_dir, "Year"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Quarter"))
#   dir.create(file.path(output_dir, "Month"))
#   dir.create(file.path(output_dir, "Week"))
#   dir.create(file.path(output_dir, "Day"))
# }
#
# dates_df <-
#   tibble::tibble(Date = seq.Date(as.Date("2026-07-26"), Sys.Date(),
#                                  by = "day")) |>
#   dplyr::mutate(Quarter = lubridate::quarter(Date),
#                 Month = lubridate::month(Date),
#                 Week = lubridate::week(Date),
#                 Day = lubridate::wday(Date, label = TRUE, abbr = FALSE, week_start = 1))
#
# test_date <- dates_df |> dplyr::pull(Date) |> dplyr::first()
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- as.Date("2026-01-01")
createYear(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir)
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createMonth(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createWeek(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
Sys.info()
dir(file.path(here::here(), "R"))
dir(file.path(here::here(), "R"), ".R")
for(sf %in% dir(file.path(here::here(), "R"), ".R")) {
for(sf in dir(file.path(here::here(), "R"), ".R")) {
source(sf)
}
dir(file.path(here::here(), "R"), ".R")
dir(file.path(here::here(), "R"), ".R", full.names = T)
for(sf in dir(file.path(here::here(), "R"), ".R", full.names = T)) {
source(sf)
}
for(sf in dir(file.path(here::here(), "R"), ".R", full.names = T)) {
source(sf)
}
for(sf in dir(file.path(here::here(), "R"), ".R", full.names = T)) {
source(sf)
}
input_dir <- here::here()
output_dir <- file.path(input_dir, "Output")
template_dir <- file.path(input_dir, "Templates")
unlink(file.path(output_dir, "*"), recursive = T, force = T)
test_date <- Sys.Date()
createYear(input_date = test_date,
template_dir = template_dir,
output_dir = output_dir,
author = "Garrett Johnson")
#
# createQuarter(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createMonth(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createWeek(input_date = test_date,
#            template_dir = template_dir,
#            output_dir = output_dir)
#
# createDay(input_date = test_date,
#            template_dir = template_dir,
#           output_dir = output_dir)
gitcreds::gitcreds_set()
usethis::use_mit_license()
usethis::create_package()
usethis::create_package(here::here())
usethis::create_package(
usethis::create_package()
usethis::create_package(here::here())
